
import { Injectable } from '@angular/core'
import { ActivatedRoute,Router } from '@angular/router'
import { ComponentStore,tapResponse } from '@ngrx/component-store'
import { WebCoreDataAccessService,MedicalConditionProvider } from '@case-clinical/web/core/data-access'
import { pluck,switchMap, tap } from 'rxjs/operators'
import { DescriptionListItem } from '@case-clinical/web/ui/description-list'
import { WebUiToastService } from '@case-clinical/web/ui/toast'

export interface MedicalConditionProviderDetailState {
  errors ?: any
  loading?: boolean
  item?: MedicalConditionProvider
}

@Injectable()
export class WebMedicalConditionProviderDetailStore extends ComponentStore<MedicalConditionProviderDetailState> {
  constructor(
    private readonly data: WebCoreDataAccessService,
    private readonly router: Router,
    private readonly route: ActivatedRoute,
    private readonly toast: WebUiToastService
) {
    super({ loading: false })
    this.loadMedicalConditionProviderEffect(route.params.pipe(pluck('medicalConditionProviderId')))
  }

  readonly errors$ = this.select((s) => s.errors)
  readonly loading$ = this.select((s) => s.loading)
  readonly item$ = this.select((s) => s.item)

readonly displayItems$ = this.select(
    this.item$,
    (item) =>
      [
{ label: 'Id', value: item?.id },
{ label: 'Name', value: item?.name },


{ label: 'Medical Records', value: item?.medicalRecords },
      ] as DescriptionListItem[],
  )

readonly tabs$ = this.select(this.item$, (item) => [
    {
      label: 'Details',
      path: 'details',
      data: item,
    },
  ])
  readonly vm$ = this.select(this.errors$, this.loading$, this.item$, this.tabs$, (errors, loading, item, tabs) => ({
    errors,
    loading,
    item: { ...item },
    tabs
  }),
{debounce: true})

  readonly loadMedicalConditionProviderEffect = this.effect<string>((medicalConditionProviderId$) =>
    medicalConditionProviderId$.pipe(
      tap(() => this.setState({ loading: true })),
      switchMap((medicalConditionProviderId) =>
        this.data.userMedicalConditionProvider({ medicalConditionProviderId }).pipe(
          tapResponse(
            (res) => this.patchState({ item: res.data.item, errors: res.errors, loading: false }),
            (errors: any) =>
              this.patchState({
                loading: false,
                errors: errors.graphQLErrors ? errors.graphQLErrors : errors,
              }),
          ),
        ),
      ),
    ),
  )

  readonly deleteMedicalConditionProviderEffect = this.effect<MedicalConditionProvider>(
    (medicalConditionProvider$) =>
      medicalConditionProvider$.pipe(
        switchMap((medicalConditionProvider) =>
          this.data
            .userDeleteMedicalConditionProvider({
              medicalConditionProviderId: medicalConditionProvider.id,
            })
            .pipe(
              tapResponse(
                (res) =>
                {  
                    this.toast.success("Deleted successfully!", {duration: 3000})
                    return this.router.navigate(['/queues/medical-condition-providers'])
                },
                (errors: any) =>
                  this.patchState({
                    loading: false,
                    errors: errors.graphQLErrors ? errors.graphQLErrors : errors,
                  }),
              ),
            ),
        ),
      ),
  )
}

