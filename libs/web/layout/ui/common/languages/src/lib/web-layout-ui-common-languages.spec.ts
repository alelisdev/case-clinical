import { webLayoutUiCommonLanguages } from './web-layout-ui-common-languages'

describe('webLayoutUiCommonLanguages', () => {
  it('should work', () => {
    expect(webLayoutUiCommonLanguages()).toEqual('web-layout-ui-common-languages')
  })
})
