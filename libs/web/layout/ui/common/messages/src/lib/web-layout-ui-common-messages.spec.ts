import { webLayoutUiCommonMessages } from './web-layout-ui-common-messages'

describe('webLayoutUiCommonMessages', () => {
  it('should work', () => {
    expect(webLayoutUiCommonMessages()).toEqual('web-layout-ui-common-messages')
  })
})
