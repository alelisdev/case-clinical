
import { Injectable } from '@angular/core'
import { WebCoreDataAccessService, ContactMethod,  } from '@case-clinical/web/core/data-access'
import { ComponentStore,tapResponse } from '@ngrx/component-store'
import { switchMap, tap, map } from 'rxjs/operators'

export interface ContactMethodFormState {
  errors?: any
  loading?: boolean
  item?: ContactMethod,

  searchTerm?: string
}

@Injectable()
export class WebContactMethodFormStore extends ComponentStore<ContactMethodFormState> {
  constructor(private readonly data: WebCoreDataAccessService) {
    super({ loading: false })
  }

  readonly errors$ = this.select((s) => s.errors)
  readonly loading$ = this.select((s) => s.loading)
  readonly item$ = this.select((s) => s.item)

  readonly vm$ = this.select(this.errors$, this.loading$, this.item$, 

    (errors, loading, item,  ) => ({
    errors,
    loading,
    item,

  }),
{debounce: true})




}
