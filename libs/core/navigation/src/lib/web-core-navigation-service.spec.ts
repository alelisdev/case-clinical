import { webCoreNavigationService } from './web-core-navigation-service'

describe('webCoreNavigationService', () => {
  it('should work', () => {
    expect(webCoreNavigationService()).toEqual('web-core-navigation-service')
  })
})
